# GENERATED by tests/_mk_dotpool_cfgs.py — do not edit by hand.
# vjepa_slotssm.yaml, scale 1, with the classifier readout PINNED to the historical mean:
#
#     slot_pool: dot
#
# THE PRE-2026-09-24 DEFAULT. On that date the base configs became gated attention
# pooling (findings 19, 22), so this file is the explicit form of what the base
# used to mean. Use it to reproduce or extend the pre-2026-09-24 arms, and as the
# scale-1 control for any future pool comparison.
#
# What 'dot' was, measured: `scores = (slots . slot_query)/sqrt(D)` with
# `slot_query` a bare `nn.Parameter(randn * 0.02)`. `||q|| = 0.02*sqrt(512) =
# 0.4525` against a slot RMS of ~1.8 puts the softmax logits within about +-0.04,
# so the pool was an ARITHMETIC MEAN -- eff 31.95-31.99 of a maximum of 32, max
# weight 0.032-0.035 against uniform 0.03125, in all ten arms of the scale sweep,
# and `||slot_query||` after 50 epochs was 0.968-1.005x its init (findings 19.1).
#
# The pool modes have different parameter NAMES, so checkpoints are NOT portable
# between them: a pre-2026-09-24 checkpoint loads into THIS config and not into
# the base.
# --------------------------------------------------------------------
# V-JEPA 2.1 + MOVAD — SlotSSM temporal model  (~21M trainable)
#
# K=32 slots × D=512, 4 blocks, Mamba2.  Matches original SlotSSM
# paper (NeurIPS 2024): same slot count, block count, and Mamba
# version.  Standard multi-head cross-attn + per-slot Mamba +
# slot self-attention.  Classifier uses learned attention-pooling.
# --------------------------------------------------------------------

# --- V-JEPA 2.1 encoder ---------------------------------------------------
model_name: vit_base
img_size: 384
num_frames: 4
tubelet_size: 2
patch_size: 16
checkpoint_path: /home/chrysenberg/vjepa2-checkpoints/vjepa2_1_vitb_dist_vitG_384.pt
checkpoint_key: ema_encoder
vjepa_spatial_grid: [6, 6]
compile: false

# --- Temporal model -------------------------------------------------------
temporal_model: slotssm
dim_latent: 1024
dropout: 0.3
num_slots: 32                       # K — matches original SlotSSM paper
slot_dim: 512                       # D — (d_model*expand/headdim)=1024/64=16, 16%8=0 ✓
num_ssm_blocks: 4                   # matches original SlotSSM paper
mamba_d_state: 128
mamba_d_conv: 4
mamba_expand: 2
mamba_version: mamba2               # matches original SlotSSM paper default
use_inverted_attention: false        # standard cross-attn (FlashMHA-compatible)

# --- Data -----------------------------------------------------------------
NF: 4
VCL: 8

data_mean: [0.5, 0.5, 0.5]
data_path: /mnt/d/Users/Chrysenberg69420/Downloads/DoTA_dataset
data_std: [0.5, 0.5, 0.5]
dataset: Dota
image_shape: [720, 1280]
input_shape: [384, 384]
FPS: 10

# --- Training -------------------------------------------------------------
batch_size: 32
class_weights: [0.3, 0.7]
epochs: 200
lr: 0.00005
snapshot_interval: 10
enable_validation: true
validation_epoch_step: 10
num_workers: 4

amp_dtype: bf16                      # bf16 (AMP mixed-precision), fp16, or fp32 (no AMP)

# --- Augmentation ---------------------------------------------------------
horizontal_flip_prob: 0.5
vertical_flip_prob: 0.0

# --- Output ---------------------------------------------------------------
output: ./output

# --- per-slot differentiation: proj ---
slot_input_proj_rank: 0    # OFF (2026-09-22): the proj arms measured
                            # WORSE than no-proj at every scale tried
                            # (findings 16.9.7 / 10.2), so proj-off is
                            # the default now. The `_proj` variants set
                            # 32 explicitly and are unaffected.

# --- slot pooling: the historical mean, PINNED -----------------------------
# An explicit pin, not an inherited default: the base configs set gated attention
# pooling from 2026-09-24.
slot_pool: dot
